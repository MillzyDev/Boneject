// gonna leave this out since it will be like 10000x better to generate this class, but I cant access the types in
// Bonelab from the source generator since im forced to use netstandard2.0 for it bc roslyn throws a hissy fit when
// I try to use .NET 5+ for it. So basically, I gotta wait for LemonLoader to support ML 6.1, because then I can use
// .NET 6 for the mod and the source generator.

/*
using SLZ.Bonelab;

namespace Boneject.Ninject.Adapters;

public partial class MainMenuAdapter : AdapterBase<GameControl_startup, GameControl_MenuVoidG114>
{
    public MainMenuAdapter(GameControl_startup obj) : base(obj)
    {
    }

    public MainMenuAdapter(GameControl_MenuVoidG114 obj) : base(obj)
    {
    }

    //public string buildTitle => AIsNotNull ? ObjA!.buildTitle : ObjB!.buildTitle;
    //public string buildState => AIsNotNull ? ObjA!.buildState : ObjB!.buildState;
    //public string buildDate => AIsNotNull ? ObjA!.buildDate : ObjB!.buildDate;
    //public TextMeshPro tmp_Title => AIsNotNull ? ObjA!.tmp_Title : ObjB!.tmp_Title;
    //public TextMeshPro tmp_BuildInfo => AIsNotNull ? ObjA!.tmp_BuildInfo : ObjB!.tmp_BuildInfo;
    //public LaserCursor mainMenuUIController => AIsNotNull ? ObjA!.mainMenuUIController : ObjB!.mainMenuUIController;
    //public GameObject logoSLZ => AIsNotNull ? ObjA!.logoSLZ! : ObjB!.logoSLZ;
}
*/


